pipeline {
    agent any


    stages {
        stage('Git Clone') {
            steps {
               git 'https://github.com/sandeepsingh07/hello-world-1.git'
            }
            }
        stage('Build'){
            steps{
                sh "mvn clean package"
            }
        }

        
    }
}
